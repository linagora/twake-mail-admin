// Minimal setup — static bearer token (prompted via modal on first request / 401)
window.__ENV__ = {
  VITE_API_BASE_URL: "http://192.168.1.155:8000",
};

// ---------------------------------------------------------------------------
// Optional OIDC/SSO setup
// If ANY SSO_ variable is present, ALL of them must be set (partial config = startup error).
// When all are set the app skips the token modal and authenticates via SSO (PKCE / auth-code flow).
// ---------------------------------------------------------------------------
// window.__ENV__ = {
//   VITE_API_BASE_URL: "http://192.168.1.155:8000",
//   SSO_BASE_URL: "https://sso.linagora.com",
//   SSO_CLIENT_ID: "twake-mail-admin",
//   SSO_SCOPE: "openid name email",
//   SSO_REDIRECT_URI: "https://twake-mail-admin.linagora.com/oidc-callback",
//   SSO_RESPONSE_TYPE: "code",
//   SSO_CODE_CHALLENGE_METHOD: "S256",
//   SSO_POST_LOGOUT_REDIRECT: "https://twake-mail-admin.linagora.com?logout=1",
// };
